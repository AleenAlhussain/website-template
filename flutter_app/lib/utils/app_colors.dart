import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color bgBase = Color(0xFF0D0D0D);
  static const Color bgCard = Color(0xFF1A1A2E);
  static const Color bgInput = Color(0xFF111124);
  static const Color bgElevated = Color(0xFF22223A);

  // Brand Accents
  static const Color gold = Color(0xFFF5C518);
  static const Color goldDim = Color(0xFFC49A0E);
  static const Color blue = Color(0xFF3D8EFF);
  static const Color emerald = Color(0xFF00C896);
  static const Color alert = Color(0xFFFF4C61);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFA0A0B8);
  static const Color textMuted = Color(0xFF5A5A72);
  static const Color textInverse = Color(0xFF0D0D0D);

  // Borders
  static const Color borderSubtle = Color(0x12FFFFFF);
  static const Color borderActive = Color(0x66F5C518);

  // Gradients
  static const List<Color> goldGradient = [Color(0xFFF5C518), Color(0xFFC49A0E)];
  static const List<Color> darkGradient = [Color(0xFF0D0D0D), Color(0xFF1A1A2E)];
  static const List<Color> blueGradient = [Color(0xFF3D8EFF), Color(0xFF0058CC)];
  static const List<Color> walletGradient = [Color(0xFF1A1A2E), Color(0xFF2A1F00)];
}
