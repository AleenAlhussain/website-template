name: myfriend_app
description: MF-MYFRIEND — Connect. Earn. Evolve.
publish_to: "none"
version: 2.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.10.0
  connectivity_plus: ^6.1.0
  google_fonts: ^6.2.1
  smooth_page_indicator: ^1.2.0
  shared_preferences: ^2.3.2
  url_launcher: ^6.3.0
  dio: ^5.7.0               # HTTP client with interceptors
  flutter_secure_storage: ^9.2.2  # encrypted token storage
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
